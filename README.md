# Volume-control-from-Hand-Gestures
I learn about the two new libraries: 1. MediaPipe, 2. PyAutoGUI. MediaPipe is used to identify objects and it’s a pre-trained model, whereas PyAutoGUI accesses your computer,to simulate pressing volume control keys.Hand Landmark Detection: Using MediaPipe, I learned to detect specific points (landmarks) on the hand.  Distance Calculation.
